services:
  - type: web
    name: trail-app
    runtime: python
    buildCommand: |
      python -m pip install --upgrade pip
      pip install -r requirements.txt
      python init_db.py
    startCommand: "gunicorn app:app"
    envVars:
      - key: FLASK_ENV
        value: production
